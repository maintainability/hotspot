package hotareadetector.data;

/**
 * Type of the operation.
 * An artificial type R (rename) is added.
 */
public enum OperationType {
	A, // add
	M, // modify
	D, // delete
	R // rename
}
